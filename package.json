{
  "name": "abolfazl-sajadi-portfolio",
  "private": true,
  "scripts": {
    "build": "npm run build:content && npm run build:assets",
    "build:content": "node scripts/render-content.mjs",
    "build:assets": "node scripts/build-assets.mjs",
    "build:site": "npm run build && node scripts/package-site.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "clean-css": "5.3.3",
    "parse5": "7.3.0",
    "sanitize-html": "2.17.7",
    "terser": "5.51.2",
    "yaml": "2.9.1"
  }
}
